exports('timer', (params, done) => {
    insertHtmlModules({}, () => {

        function getTime() {
            /*Делаем запрос на FI и получаем DC, TU, TT, TM*/
        }
        

      done();
    });
  });